rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /profile_pictures/{imageFile} {
      allow read;
      allow write: if request.auth != null && imageFile == request.auth.uid + '.jpg';
    }
  }
}
